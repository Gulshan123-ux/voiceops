# ─────────────────────────────────────────────
# VoiceOps Sentinel – Pinned Requirements
# Python 3.11+ (tested on 3.13)
# Generated: 2026-06-17
# ─────────────────────────────────────────────

# ── API & Web Framework ───────────────────────
fastapi==0.137.1
uvicorn[standard]==0.49.0
python-multipart==0.0.32        # Required for FastAPI file uploads

# ── OpenAI Whisper (primary ASR) ─────────────
openai>=1.30.1                  # Install separately: pip install openai

# ── Deepgram (fallback ASR) ──────────────────
deepgram-sdk>=3.2.7             # Install separately: pip install deepgram-sdk

# ── Audio Processing ─────────────────────────
pydub==0.25.1
# ffmpeg must be installed via system package manager: brew install ffmpeg

# ── WER Evaluation ───────────────────────────
jiwer==4.0.0

# ── Pydantic / Validation ────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1

# ── Config ───────────────────────────────────
python-dotenv==1.2.2

# ── Retry / Back-off ─────────────────────────
tenacity==9.1.4

# ── Testing ──────────────────────────────────
pytest==9.1.0
pytest-asyncio==1.4.0
httpx==0.28.1                   # AsyncClient for FastAPI testing
anyio==4.14.0

# ── Linting ──────────────────────────────────
flake8>=7.0.0

# ── Utilities ────────────────────────────────
aiofiles==25.1.0
